# Copy to .env and fill in to enable the optional LLM judge channel.
# RecRedTeam reads these standard user-oriented variables (not agent runtime vars).
USER_LLM_API_KEY=
USER_LLM_BASE_URL=https://api.openai.com/v1
USER_LLM_MODEL=gpt-4o-mini
